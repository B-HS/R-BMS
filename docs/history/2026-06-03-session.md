# 2026-06-03 — 위생·lntype·#PREVIEW·CN/HCN·NETWORK·Windows 경로 (세션 이력)

> 9-에이전트 적대적 분석/비평으로 도출한 로드맵을 실행한 세션. 진행 순서·확정 순서는 `docs/PROCESS.md` "현재 작업"·`ROADMAP.md`. 결과: **테스트 880 → 887 통과·0 실패·0 경고**, 전부 dev 커밋·푸시(co-author 없음).

## 작업·검증 흐름 (커밋 단위)
- `1d3fb13` **Phase 0 위생** — 미커밋 `docs/` 전체 + 루트 `LICENSE`(GPL-3.0 전문, gnu.org 원문) 커밋. 테스트 수 정정(`132/92/100 → 880`, PROCESS·CLAUDE·ci-release). `ci-release.md`·README·ROADMAP의 원격/브랜치/배포 stale 정정(origin `B-HS/R-BMS`·`dev` 푸시됨 반영). dev 푸시 → 3-OS CI green.
- `a9ff66e` **Phase 1** — IR `lntype`를 차트 `#LNMODE`에서 유도(`ir_map::ir_lntype` 0=LN/1=CN/2=HCN, `App.chart_lntype`; 기존 `lntype:1` 하드코딩 제거, 단위테스트). `#PREVIEW` `start_preview`의 6개 silent early-return에 `config.debug` 계측, `samples/preview-demo`(.bms+wav) 픽스처, `SongEntry.preview`의 stale `#[allow(dead_code)]` 제거(이제 읽힘).
- `ca80918` **Phase 3** — NETWORK 설정 탭(SERVER URL/PLAYER ID 행 22/23, `text_input` 인플레이스 편집·`PlaySettings.server_url/player_id` 영속·라운드트립 테스트·`build_server` 추출+`rebuild_server`). 기존 CLI 전용(`--server`/`--player`) 값을 GUI로.
- `0c1b36b` **Phase 4 게이트** — `docs/backend/contract-freeze.md`(클라 실제 경로 대조·계약 3건·envelope·MySQL/인증 결정 + M0 단계).
- `161ecf2` **CN/HCN 판정** — `matcher.rs`가 `LnKind` 운반(`JNote.ln`·`from_triples`), `is_charge` 게이트. CN/HCN = head@press + end@release **2-판정**(분모 2, `count_playable_notes`·`total_notes` 일치), LN = `worse(head,end)` 1-판정 불변. 합성 픽스처 5종. → `docs/reference/cn-hcn-judgment.md`.
- `b5cff35` **Windows 설정 경로** — `config_dir` = `HOME → USERPROFILE → "."`(기존 HOME만 → Windows cwd 추락 버그). `config_dir_from` 단위테스트. → `docs/reference/windows-compat.md`.

## 사용자 결정 (세션 중)
- **전략 순서**: 위생→릴리스→CN/HCN→마무리 후 백엔드 피벗(권장 채택).
- **릴리스**: **보류**(공개 `v0.0.1` 자동 트리거 회피). 준비 = `docs/acknowledge/release-branch-strategy.md`(v0.1.0 수동태그+prod 미보호 권장).
- **CN/HCN**: **스펙대로 구현 + 합성 픽스처**(채택, 위 `161ecf2`).
- **백엔드**: **보류**(별 MIT 레포·MySQL·인증 소유권). 준비 = `contract-freeze.md`.

## 미검증/잔여 (정직)
- `#PREVIEW` **가청 확인**: 헤드리스 불가 → 실기에서 DEBUG MODE on + `./start.sh samples/preview-demo`로 `[preview]` 로그/소리 확인(수동 1회).
- CN/HCN **노트-카운트 분모**: beatoraja BMS 모델이 외부 라이브러리라 원본 대조 불가 → `JudgeManager` 2×`updateMicro` 증거 + 내부 일관 기반.
- **HCN 연속 게이지·CN deferral/재홀드·스크래치 BSS** = Phase 7.
- **Windows GUI/오디오/폰트 런타임** = 실기 검증 필요(`windows-compat.md`).

## 산출 신규 문서
`docs/reference/cn-hcn-judgment.md` · `docs/reference/windows-compat.md` · `docs/backend/contract-freeze.md` · `docs/acknowledge/release-branch-strategy.md` · `docs/bug/2026-06-03-preview-playback.md` · (본 파일).
