# Phase F — 화면·설정 확장 UX 검증 (2026-09-10)

> 근거: `docs/plan/2026-09-09-phase-f-spec.md`(F0~F4 작업 정의), `docs/acknowledge/2026-09-09-enhancement-decisions.md`(결정 3: 옵션 오버레이가 메인 경로 / 결정 6: Esc 홀드·더블 옵션), `docs/history/2026-09-10-phase-f-integration.md`(F0~F4 갈래 합류·교차 배선), `docs/acknowledge/reference-divergences.md`(§Phase F). 이 문서는 통합 단계 이후 오케스트레이터가 돌린 **최종 검증 단계**(코드 수정 없음, `cargo fmt --all`만 적용)의 결과를 정리한다.

## 대상 범위

F0(선행 배관) ~ F4(로딩·백그라운드·셸)까지 다섯 갈래가 합류한 상태의 워크트리. 소유권표 기준 실제 파일은 `apps/rbms-player/src/{app_options,app_result,dialog,favorites,notify,textedit,toast,target}.rs`, `apps/rbms-player/src/stage/{play/,select/{filter,list,tests}.rs,render_tests_*.rs}`, `crates/rbms-config/src/{options,sort}.rs`, `crates/rbms-render/src/{result/,toast.rs}`, `crates/rbms-render/tests/golden_*.rs` 등이다.

## 게이트 결과

| 항목 | 결과 |
|---|---|
| `cargo fmt --all -- --check` (수정 전) | 통과 — 재포맷할 라인 없음 |
| `cargo fmt --all` | no-op (변경 파일 0) |
| `cargo build --workspace --all-targets` | 통과 |
| `cargo clippy --workspace --all-targets -- -D warnings` | 통과, 경고 0 |
| `cargo test --workspace` | **테스트 바이너리 38개 전부 `test result: ok`, 2232 passed / 0 failed / 2 ignored** |
| 금지어(레퍼런스 구현 실명) grep | 0건 |
| 신규 추가 라인 중 `//`/`/* */` 주석 | 0건(모두 `//!`/`///` 문서주석이거나 URL 문자열 리터럴의 `://`) |
| `git status --short` | 워킹트리는 FIX 패스가 만든 상태 그대로(수정 47개 트래킹 파일 + 신규 파일들, 삭제 2건) — 이번 검증 단계는 코드에 순변경 없음 |

`cargo test --workspace` 세부 집계: `rbms_config` 253(+1 무시), `rbms_player` lib 564 / bin 9 / autoplay_preview 44, `rbms_render` lib 182 + `golden_play` 6 + `golden_result` 8 + `golden_select` 4 + `golden_shell` 6, 그 외 `rbms_audio`/`rbms_chart`/`rbms_ir`/`rbms_judge`/`rbms_library`/`rbms_model`/`rbms_parser`/`rbms_play`/`rbms_store`/`rbms_table` 정상. 기존 단일 `crates/rbms-render/tests/golden.rs`가 화면별 `golden_play.rs`/`golden_result.rs`/`golden_select.rs`/`golden_shell.rs` 4개로 분리된 상태이며 전부 green이다 — 골든 시그니처(`GOLDEN_RESULT`/`GOLDEN_RESULT_SKIN`/`GOLDEN_SELECT`/`GOLDEN_HUD` 계열)가 이번 검증 시점에도 깨지지 않았다.

## 헤드리스 렌더 확인 (임시 하네스, 검증 후 제거)

`CpuCanvas` 기반 헤드리스 렌더로 세 화면을 PNG로 캡처해 육안 확인했다. 캡처에 쓴 코드(`HeadlessCanvas`에 임시 `rgba_for_tests`/`dims_for_tests` 접근자, `OptionsOverlay::open_panel_for_tests`, 임시 테스트 모듈)는 렌더 결과 확인 후 전부 되돌렸고, 최종 워킹트리에는 남아 있지 않다.

| 캡처 | 내용 | 확인된 것 |
|---|---|---|
| `option_overlay.png` | 곡선택 화면 위에 옵션 오버레이(F2-1, 결정 3) | RANDOM/GAUGE/HI-SPEED/HI-SPEED FIX/LANE COVER/LIFT/HIDDEN+/SCRATCH SIDE/SCRATCH AUTO/AUTOPLAY/TARGET 11행 + 하단 키 힌트(UP DOWN MOVE·LEFT RIGHT CHANGE·F1 PIN·ESC CLOSE)가 그려진다 |
| `result_with_graphs.png` | 결과 화면(F3), `show_result_graphs: true` | 등급 문자(A-)·27분위 눈금 랭크바·EX SCORE/MAX COMBO/TOTAL NOTES·6단 판정 막대(PERFECT~MISS)·FAST/SLOW·GAUGE, 그리고 하단 GAUGE/TIMING/JUDGE 3분할 그래프 패널(빈 시계열이라 "NO DATA" 표시)이 함께 그려진다 |
| `select_filters_and_toast.png` | 곡선택 화면, 필터 패널(F1-4) 열림 + 토스트(F4-1) 동시 표시 | 상단 바에 `FILTER: FAVOURITES` 배지, 좌측에 LEVEL FROM/TO·MODE·CLEAR·FAVOURITES(STARRED ONLY) 5행 필터 패널, 우하단에 에러 토스트("chart not found: /songs/missing.bms")가 화면 아래를 가리지 않고 겹쳐 그려진다 |

PNG 원본은 `/private/tmp/claude-501/-Users-gkn-R-BMS/847c1230-e223-4d0c-adb0-2d6113bbb669/scratchpad/phase-f-shots/`에 보존했다(세션 스크래치 디렉터리 — 저장소에는 커밋하지 않는다).

## FIX 패스 요약 (참고, 이번 단계 이전에 완료)

이번 검증 직전 세션에서 적대적 리뷰 21건 중 19건을 코드로 수정하고 2건(BPM 정렬 축, 차트 파싱 워커화)을 `docs/acknowledge/reference-divergences.md`에 사유·해소 조건과 함께 등록했다. critical 1건(`target.rs`의 `band_target_ex` 2단 나눗셈 off-by-one, 밴드 9/18/23에서 총 582건 불일치 실측)을 포함해 major 다수(FAST/SLOW 리플레이 집계 0 회귀, 옵션 오버레이가 PLAY까지 살아남던 문제, SELECT SORT 설정 무동작, LANE COVER 100%+SPEED FIX 조합의 hispeed 스냅 등)가 이 단계 이전에 이미 반영되어 있었고, 이번 검증에서 그 결과가 전부 green임을 재확인했다.

## 결론

Phase F(F0~F4) 코드는 `fmt`/`build`/`clippy -D warnings`/`test --workspace` 4개 게이트를 전부 통과하고, 금지어·금지 주석 패턴도 0건이다. 옵션 오버레이·결과 그래프·곡선택 필터+토스트 세 화면 모두 헤드리스 렌더로 실제 픽셀 출력을 확인했다. 남은 미구현·의도적 이탈 항목은 `docs/acknowledge/reference-divergences.md` §Phase F에 전부 등록되어 있다.
