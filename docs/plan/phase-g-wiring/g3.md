# G3 코스 — 배선 지시 (색인)

**정본은 [`G3-course.md`](./G3-course.md) 다.** 이 파일은 README 색인이 가리키는 이름이라 남겨 둔
포인터이며, 아래 표는 정본의 항목 목록이다. 실제 코드 블록·앵커·근거는 전부 정본에 있다.

| 정본 절 | 파일 | 내용 |
|---|---|---|
| §1.1 | `crates/rbms-play/src/session.rs`, `crates/rbms-judge/src/gauge.rs` | **차단 항목** — 게이지·콤보 이월 API 부재. 두 크레이트는 Phase G 의 어느 갈래도 소유하지 않으므로 G8 이 소유권 확인 후 넣는다 |
| §1.2 | — | NO GOOD / NO GREAT 는 저장·표시만(스펙 §12 R7) |
| §1.3 | `apps/rbms-player/src/course_ui.rs` | G1 곡DB 이후 `library_index` 만 sha256 우선으로 |
| §2 | `apps/rbms-player/src/lib.rs` | `AppShared` 코스 필드 3개, `start_course`/`load_course_stage`/`end_course` |
| §3 | `apps/rbms-player/src/stage/mod.rs`, `stage/course_result.rs` | `Stage::CourseResult` + `StageId::CourseResult`. `Stage::Course` 는 만들지 않기를 권고 |
| §4 | `apps/rbms-player/src/stage/select/**` | `SelectTab` 탭, `CourseList` 적재·키·그리기 |
| §5 | `apps/rbms-player/src/app_result.rs` | `enter_result` 코스 분기, `advance_course`, `finish_course`(IR 제출) |
| §6 | `apps/rbms-player/src/stage/loading.rs` | 코스 진행 표시, 게이지·콤보 이월 주입 |
| §7 | `apps/rbms-player/src/app_input.rs`, 플레이 화면 | 하이스피드 잠금, 코스 중 Esc |
| §8 | `docs/**` | `rbms-course` 크레이트 등재, 미적용 항목의 이탈 기록 |
| §9 | — | G3 가 스펙 대비 내린 판단 10건 |
